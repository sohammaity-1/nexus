spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nexus
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

server:
  port: 8080